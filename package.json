{
  "name": "egg-diet-tracker",
  "version": "1.0.0",
  "description": "15-day egg diet tracker iOS app",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js",
    "sync": "npm run build && npx cap sync",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "express": "^5.2.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
